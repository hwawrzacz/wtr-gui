export enum CreationResponseMessage {
  INVALID_DUTY_DATE = 'Duty date is invalid',
  LOGIN_IS_TAKEN = 'Login is taken',
  PROJECT_VALIDATION_FAILED = 'projects validation failed',
}

export enum DeletionResponseMessage {
  INVALID_DUTY_DATE = 'Duty date is invalid',
  LOGIN_IS_TAKEN = 'Login is taken',
  PROJECT_VALIDATION_FAILED = 'projects validation failed',
}

export enum GetResponseMessage {
  INVALID_DUTY_DATE = 'Duty date is invalid',
  LOGIN_IS_TAKEN = 'Login is taken',
  PROJECT_VALIDATION_FAILED = 'projects validation failed',
}